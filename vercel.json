{
    "builds": [{
        "src": "verceldjango/wsgi.py",
        "use": "vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "verceldjango/wsgi.py"
        }
    ]
}